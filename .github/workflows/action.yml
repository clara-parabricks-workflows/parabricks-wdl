name: 'Clara Parabricks WDL'
description: 'Validate workflows in the repo'
on:
  push:
  pull_request:
  
inputs:
outputs:
runs:
  using: 'docker'
  image: 'clara-parabricks/cromwell'
  args:
    - "make validate WOMTOOL=/usr/bin/womtool.jar"